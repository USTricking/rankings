---
type Props = {
	size?: number;
	width?: number;
	height?: number;
	src?: string;
};
const { size, width, height, src } = Astro.props;
---
<img src={src ?? "/ustricking_logo.png"} alt="Logo" width={size ? size : (width ?? 48)} height={size ? size : (height ?? 48)} />
